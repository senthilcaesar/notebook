# My Notebook Flashcards

Project documentation lives in **[AGENTS.md](AGENTS.md)** — read it before
changing anything in this repo.

It is the single source of truth for layout, the data model, the card design
rules, and the conventions that are easy to break silently (memoised
`Flashcard`, the breakpoint-free grid, `toSafeHref` on user URLs, the exact
`firebase` pin).

This file is intentionally a pointer. The two docs previously held duplicate
content and drifted until both described an app that no longer existed.
